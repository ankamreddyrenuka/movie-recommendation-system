services:
  - type: web_service
    name: traveldna
    env: java
    plan: starter
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/traveldna-1.0.0.jar
    autoDeploy: true
    # No external API keys required for seeded demo
    routes:
      - type: web
