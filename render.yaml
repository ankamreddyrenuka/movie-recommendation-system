services:
  - type: web_service
    name: traveldna
    env: java
    plan: starter
    buildCommand: mvn clean package -DskipTests
    # Use wildcard to avoid failures if the jar name changes slightly
    startCommand: java -jar target/*.jar

    autoDeploy: true
    # No external API keys required for seeded demo
    routes:
      - type: web
